# 🤖 КарточкаPRO — Telegram-бот для генерации карточек товаров

AI-бот для создания SEO-оптимизированных карточек товаров на Wildberries и Ozon.

## Возможности

- 🛍 **Генерация карточек** — заголовок, описание, ключевые слова, характеристики
- 🤔 **Умные вопросы** — AI задаёт уточняющие вопросы под конкретный товар
- 🔍 **Анализ конкурентов** — разбор и улучшение карточки конкурента
- ✨ **4 стиля подачи** — премиум, бюджетный, молодёжный, деловой
- 📂 **История карточек** — все генерации сохраняются с пагинацией
- 🎁 **Реферальная программа** — 3 дня Pro за каждого приглашённого
- 🔔 **Напоминания** — автоуведомления неактивным пользователям (3 дня)
- 💎 **Подписки** — free / standard / pro с лимитами
- 📊 **Админ-панель** — статистика, подписки, рассылки

## Требования к серверу

- Ubuntu 24.04 LTS
- 1 ядро CPU, 1 ГБ RAM, 20 ГБ NVMe
- Docker + Docker Compose

## Быстрый старт

### 1. Получите токены

**Telegram Bot:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`, задайте имя и username
3. Скопируйте токен

**ProxyAPI (OpenAI):**
1. Зарегистрируйтесь на [proxyapi.ru](https://proxyapi.ru)
2. Пополните баланс
3. Создайте API-ключ в личном кабинете

### 2. Загрузите на сервер

```bash
# Скопируйте проект на сервер (scp, git clone, или просто создайте файлы)
cd /opt
mkdir kartochka-bot
cd kartochka-bot
# ... скопируйте все файлы проекта сюда
```

### 3. Настройте окружение

```bash
cp .env.example .env
nano .env
```

Заполните:
```
BOT_TOKEN=ваш-токен-от-BotFather
ADMIN_IDS=ваш-telegram-id
OPENAI_API_KEY=ваш-ключ-от-proxyapi
```

Узнать свой Telegram ID: напишите [@userinfobot](https://t.me/userinfobot)

### 4. Запустите

```bash
chmod +x deploy.sh
./deploy.sh
```

Или вручную:
```bash
mkdir -p data
docker compose build
docker compose up -d
```

### 5. Проверьте

```bash
docker compose logs -f
```

Откройте бота в Telegram и отправьте `/start`.

## Структура проекта

```
kartochka-bot/
├── bot/
│   ├── main.py              # Точка входа + планировщик
│   ├── config.py             # Конфигурация из .env
│   ├── database/
│   │   └── db.py             # SQLite — пользователи, генерации, рефералы, лимиты
│   ├── services/
│   │   ├── ai_service.py     # OpenAI API — промпты и генерация
│   │   └── scheduler.py      # Напоминания неактивным пользователям
│   ├── handlers/
│   │   ├── start.py          # /start, меню, профиль, тарифы, рефералы
│   │   ├── generate.py       # Генерация карточек, анализ конкурентов
│   │   ├── history.py        # «Мои карточки» с пагинацией
│   │   ├── subscription.py   # Оплата и подписки
│   │   ├── admin.py          # Админ-команды
│   │   └── fallback.py       # Обработка неизвестных сообщений
│   ├── keyboards/
│   │   └── inline.py         # Все клавиатуры
│   └── middlewares/
│       └── throttle.py       # Антиспам
├── data/                     # БД (создаётся автоматически)
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── .env.example
├── deploy.sh
└── README.md
```

## Команды бота

### Пользовательские
- `/start` — запуск бота
- `/menu` — главное меню

### Админские (только для ADMIN_IDS)
- `/admin` — статистика
- `/activate user_id plan` — активировать подписку (standard/pro/free)
- `/userinfo user_id` — инфо о пользователе
- `/broadcast текст` — рассылка всем пользователям

## Управление

```bash
# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Пересборка после изменений
docker compose up -d --build

# Бэкап БД
cp data/bot.db data/bot.db.backup
```

## Настройка оплаты

На старте бот работает с ручным подтверждением оплаты:
1. Пользователь выбирает тариф → видит реквизиты для перевода
2. Переводит деньги → нажимает «Подтвердить»
3. Админ получает уведомление → проверяет перевод
4. Админ вводит `/activate user_id standard` → подписка активна

**Измените реквизиты карты** в файле `bot/handlers/subscription.py` (переменная `PAYMENT_INFO`).

### Подключение ЮKassa (следующий шаг)
Для автоматической оплаты подключите ЮKassa:
1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. Получите shop_id и секретный ключ
3. Добавьте в `.env` и доработайте `subscription.py`

## Расход ресурсов

На сервере 1 ядро / 1 ГБ RAM бот потребляет:
- **RAM:** ~80-150 МБ (Docker + Python + SQLite)
- **CPU:** <5% в обычном режиме
- **Диск:** ~200 МБ (Docker образ) + БД (растёт медленно)

## Стоимость API

При использовании ProxyAPI и модели gpt-5-mini:
- 1 карточка ≈ **0.5 ₽**
- 100 пользователей × 50 карточек/мес = 5000 карточек = **~2500 ₽/мес**
- Доход: 100 × 490 ₽ = **49 000 ₽/мес**
